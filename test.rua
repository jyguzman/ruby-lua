function calculate(x, y)
  local sum = x + y;
  val product = x * y;
  if x > y then
    return sum;
  else
    return product;
  end
end

for i = 1, 10 do
  local result = calculate(i, 2);
  print("Result:", result);
end

status = true and false or not false;
data = [1, 2, 3, 4];
info = {name = "Lua", type = "Language"};